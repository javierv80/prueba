<% provide(:title, 'Mis preguntas') %>

<h1>Mis Preguntas</h1>

<% if @questions.any? %>
	<ul>
		<%= render @questions %>
	</ul>

	<%= will_paginate %>
<% else %>
	<p>No has realizado ninguna pregunta aun</p>
<% end %>