<%= form_for(@question) do |f| %>
	<%= render 'common/form_errors', object: @question %>
	<p>
		<%= f.label :body, "Pregunta" %><br />
		<%= f.text_field :body %>

		<%= f.submit "Pregunta!" %>
	</p>

<% end %>